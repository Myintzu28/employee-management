import { SecurityContextService } from './security-context-service';

describe('SecurityContextService', () => {
  it('should create an instance', () => {
    expect(new SecurityContextService()).toBeTruthy();
  });
});
